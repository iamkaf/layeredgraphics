# layered-graphics-cli

The native `lg` command-line interface for [Layered Graphics](https://github.com/iamkaf/layeredgraphics).

```bash
cargo install layered-graphics-cli
lg --help
```

The CLI creates, edits, inspects, validates, diffs, watches, and renders portable `.kgfx` documents. It is experimental alpha software.
